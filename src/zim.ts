import bindings from 'bindings';

const {
  Article: ZimArticle,
  ZimCreatorWrapper,
  ZimReaderWrapper,
} = bindings('zim_binding');

export {
  ZimArticle,
  ZimCreatorWrapper,
  ZimReaderWrapper,
}
